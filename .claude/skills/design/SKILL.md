---
name: design
description: F0 설계 단계(분석·결정·설계 문서·설계 점검)를 실행한다.
disable-model-invocation: true
---

# /design — F0 설계

- 과제 원문을 분석해 결정이 필요한 지점을 드러낸다
- 사용자 결정을 받는다
- 결정을 입력으로 설계 문서와 task_list를 만든다
- 설계 점검까지 돌린다

이 스킬은 메인 세션에서 아래 순서를 **그대로** 실행한다.

- 순서를 바꾸거나 단계를 건너뛰지 않는다
- `🛑 멈춤`이 나오면 그 자리에서 사용자에게 보고하고 응답을 기다린다

시간 목표 (과제 시작 기준)

- 분석: +0:15
- 결정: +0:25
- 설계 문서 · 점검: +1:00

## 0. 사전 확인

- `docs/design/00-assignment.md`의 "원문" 아래가 비어 있으면
  - → 🛑 멈춤: "원문을 붙여넣어 주세요"
- `main`에서 `feature/design` 브랜치를 만든다
  - 이미 있으면 그 브랜치로 이동한다
  - 붙여넣은 원문(커밋 안 된 변경)은 브랜치에 그대로 따라온다
- `docs/task_list.md` 시간 계획의 `**시작:**` 줄에 현재 시각을 적는다
- 원문을 커밋한다
  - 대상: `00-assignment.md` + `task_list.md`
  - 메시지: `docs: 과제 원문 추가`
  - task_list T0-1 체크를 같은 커밋에 넣는다
- 그 뒤에도 커밋 안 된 변경이 남아 있으면
  - → 🛑 멈춤: 변경 목록을 보여주고 처리 방법을 묻는다

## 1. 분석

- `analyst`를 호출한다
  - 지시: "`00-assignment.md`를 분석해 `00-analysis.md`를 채운다"
  - 원문 문장이 40개를 넘으면 두 번에 나눠 호출한다
    - 1차 §0 ~ §3 / 2차 §4 ~ §6
- 끝나면 `00-analysis.md`를 직접 훑는다
  - 템플릿 자리표시(`…`, 빈 `S-` · `H-` · `C-`)가 남았는지
  - "추천" · "권장"처럼 결론으로 기운 표현이 없는지
  - 문제가 있으면 `analyst`에게 한 번 더 고치게 한다
- 커밋한다
  - 메시지: `docs: 요구사항 분석`
  - task_list T0-2 체크를 같은 커밋에 넣는다

## 2. 결정 받기

🛑 멈춤: 아래 형식으로 보고하고 결정을 받는다.

- `C-n`마다 선택지를 그대로 보여준다
  - 결론이나 추천을 덧붙이지 않는다
  - 가능하면 AskUserQuestion으로 항목별 선택지를 준다
  - 한 번에 4개씩 묻는다
- 사용자가 "나중에"라고 한 항목은 보류로 기록한다
  - §7 표기: `C-n → 보류 (이유)`
  - 보류 항목에 기대는 요구사항은 3단계에서 `(D-n 대기)`로 표시된다

```
## 분석 완료
- 원문 문장 S-1 ~ S-n
- 숨은 요구사항 H-1 ~ H-n
- 확인 필요 n건

## 평가 비중 상위
1. …

## 포기하면 감점이 큰 것
- …

## 결정이 필요한 항목 (C-1 ~ C-n)
### C-1. 쟁점
- 관련: S- · H-
- A: … — 장점 / 단점
- B: … — 장점 / 단점
```

받은 결정은 `00-analysis.md` §7 "사용자 결정 기록"에 적는다.

- 형식: `C-n → 선택 (사용자가 덧붙인 이유가 있으면 함께)`
- 커밋하지 않는다. 5단계에서 함께 커밋한다

## 3. 설계 문서 작성

두 번에 나눠 호출한다. 한 번에 네 문서를 쓰면 길어서 중단 시 상태를 알 수 없다.

- 1차: `doc-writer`를 호출한다
  - 지시: "분석과 §7 결정을 입력으로 `01-requirements.md`를 작성한다"
  - 끝나면 `01`을 직접 훑는다
    - 자리표시가 남았는지
    - `(D-n 대기)`가 보류 결정과 맞는지
- 2차: `doc-writer`를 호출한다
  - 지시: "`01`을 기준으로 `02` → `03` → `task_list` 순서로 작성한다"
  - task_list는 F1 이하만 채운다
    - F0 블록 · 시간 계획의 F0 행 · **현재** 줄은 건드리지 않는다
- 보고에 "새로 필요한 결정"이 있으면
  - → 🛑 멈춤: 2단계와 같은 형식으로 묻는다
  - 받은 결정을 `00-analysis.md` §4에 `C-n`으로 추가하고 §7에 기록한다
  - `doc-writer`를 다시 호출한다
    - 지시: "이어서 작성한다. 이미 채워진 절은 건너뛴다"
- 끝나면 네 문서를 직접 훑는다
  - 템플릿 자리표시가 남았는지 확인한다
    - `{기능명}` 같은 `{…}` · 빈 표 행
    - 예시 값
      - `Xxx` · `XXX_` · `PARENT` · `CHILD`
      - `/resources` · `name` · `amount` · `items`

## 4. 설계 점검

- `verifier`를 **설계 점검 모드**로 호출한다
- 높음 지적이 있으면 `doc-writer`로 고친다
  - 고친 뒤 `verifier`를 한 번 더 돌린다 (최대 2회)
- 아래 중 하나면 → 🛑 멈춤: 지적과 선택지를 보고한다
  - 2회 뒤에도 높음이 남는다
  - 고치려면 새 결정이 필요하다
    - 새 결정은 `00-analysis.md`에 `C-n`으로 추가하고 §7에 기록한다
    - 3단계(`doc-writer` 수정)로 돌아간다
    - `verifier` 재실행 횟수는 합산 2회다

## 5. 커밋

task_list의 F0 작업 줄 문장으로 문서별 커밋을 만든다.

- task_list는 부분 스테이징이 어려우므로 마지막 커밋에서만 add한다
- T0-3 ~ T0-5 체크박스는 마지막 커밋에 몰아 넣는다

- `docs: 요구사항 구체화 및 테스트 케이스 작성`
  - `01` + `00-analysis.md` §7 결정 기록
- `docs: 도메인 모델 및 ERD 작성` — `02`
- `docs: API 명세 및 에러 코드 작성` — `03`
- `docs: 작업 목록 작성`
  - `task_list` (T0-3 ~ T0-6 체크 포함)
  - 시간 계획의 F0 실제 종료 시각 포함
  - **현재**를 `F1 / T1-1`로 옮긴다

## 6. 마무리

🛑 멈춤: 아래를 보고하고 PR을 올릴지 묻는다.

```
## F0 설계 완료 (소요 hh:mm / 목표 1:00)
- 분석: S n / H n / C n
- 문서: FR n / NFR n / TC n / API n / ErrorCode n
- 결정: D-1 …, D-2 …
- 보류: (D-n 대기 항목, 없으면 "없음")
- 범위 밖으로 둔 H: …
- 설계 점검: 높음 0 / 중간 n / 낮음 n (남긴 지적 목록)
- 기능 순서: F1 … → F2 … → …

PR을 올릴까요? (push → PR 생성. 머지는 GitHub에서 직접)
```

승인하면:

- `.claude/skills/run-feature/pull-request.md` 절차로 PR을 올린다
  - 대상 브랜치: `feature/design`
- 다음 명령은 `/run-feature F1`이다
