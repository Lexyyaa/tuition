# 요구사항 구체화

> 아래 두 문서를 구현 가능한 단위로 쪼갠다.
> - 과제 원문: [00-assignment.md](00-assignment.md)
> - 분석: [00-analysis.md](00-analysis.md)
>
> 이 문서가 **요구사항 ID와 테스트 케이스 ID의 유일한 출처**다.

## ID 규칙

| 접두 | 뜻 | 형식 | 예 |
|---|---|---|---|
| `FR` | 기능 요구사항 | `FR-{F}.{항}` | `FR-2.1` |
| `NFR` | 비기능 요구사항 (동시성·정합성·기술 조건) | `NFR-{항}` | `NFR-3` |
| `SUB` | 제출물 요구사항 | `SUB-{항}` | `SUB-2` |
| `D` | 모호한 점에 대한 결정 | `D-{항}` | `D-4` |
| `TC` | 테스트 케이스 | `TC-{F}-{번호}` | `TC-2-07` |

- ID는 재사용하지 않는다. 삭제하면 번호를 비워 둔다
- `{F}`는 `task_list`의 기능(F) 번호와 같다
  - F1 기초 설정은 `FR`이 없고 `TC-1-xx`만 있다 (값 객체 · 공통 검증)
  - 첫 도메인 기능은 F2 → `FR-2.x` · `TC-2-xx`
- 원문 한 문장 = 요구사항 한 행
  - 한 문장에 규칙이 둘이면 행을 나눈다
- 원문에 없는데 필요해서 추가한 요구사항
  - `원문` 열에 `(파생 H-n)`이라 적는다
  - 필요하면 `D`로 근거를 남긴다
- 분석 문서의 ID를 출처로 쓴다
  - `S-n` 원문 문장
  - `H-n` 숨은 요구사항
  - `C-n` 결정 항목 → 사용자가 정하면 §5 `D-n`

---

## 1. 과제 범위

**필수**
- 

**선택**
- 

**범위 밖** (구현하지 않는다 — 면접에서 물어보면 여기가 근거)
- 인증·인가 (식별은 헤더로 시뮬레이션)
- H- : {반영하지 않은 숨은 요구사항} — 이유

---

## 2. 기능 요구사항

<!--
기능 하나당 아래 블록을 복사한다.
블록 번호 = task_list의 기능(F) 번호.
-->

### F1. 기초 설정

<!-- FR 없음. 공통 값 객체 · enum · seed의 TC만 둔다 -->

| TC | 분류 | 대상 | 상황 (Given / When) | 기대 결과 (Then) | FR |
|---|---|---|---|---|---|
| TC-1-01 | 성공 | Domain | | | — |
| TC-1-02 | 실패 | Domain | | `IllegalArgumentException` | — |

### F2. {기능명}

| ID | 원문 | 구체화 (구현 기준) | 구분 |
|---|---|---|---|
| FR-2.1 | S- | | 필수 |
| FR-2.2 | S- | | 필수 |

**상세 정책**

<!--
표 한 칸에 안 들어가는 규칙만 적는다.
- 예: 선정 순서, 계산식, 허용 조건, 범위 밖 처리 등
불릿 하나에 규칙 하나.
여러 해석이 가능했던 것은 여기 쓰지 말고 5절 D로 보낸다.
-->

- 

**테스트 케이스**

<!--
분류: 성공 / 실패 / 엣지 / 동시성
대상: Domain(단위) / Service(통합) / API(MockMvc)

엣지·실패 케이스 발굴 체크 (해당 없으면 건너뛴다)
- 경계값: 0, 최솟값, 최댓값, 기간 시작일·종료일 당일
- 입력: null, 빈 값, 빈 리스트, 음수, 형식 오류, 길이 초과
- 리스트 입력: 같은 요소 중복, 요소 내부 필드 검증(@Valid 캐스케이드)
- 존재: 없는 리소스, 다른 사람의 리소스
- 상태: 상태별로 가능/불가능한 행위, 이미 종결된 건 재처리
- 원자성: 중간 실패 시 전체 롤백, 부분 저장 없음
- 외부 I/O: 파일·외부 호출 실패 시 DB와의 정합
- 동시성: 같은 요청 동시 N건, 같은 자원 경합
- 입력 오류가 500으로 새지 않는가 (전부 4xx여야 한다)

Then(기대 결과)에는 상태코드만 쓰지 않는다.
응답 본문의 핵심 필드와 DB 상태(건수·값)까지 적는다.
-->

| TC | 분류 | 대상 | 상황 (Given / When) | 기대 결과 (Then) | FR |
|---|---|---|---|---|---|
| TC-2-01 | 성공 | API | | | FR-2.1 |
| TC-2-02 | 실패 | Domain | | `ErrorCode.XXX`, 저장 0건 | FR-2.2 |
| TC-2-03 | 엣지 | Domain | | | |
| TC-2-04 | 동시성 | Service | 같은 요청 N건 동시 | | NFR-1 |

---

## 3. 비기능 요구사항

<!--
과제 원문의 기술 조건 + 기능에서 드러나는 동시성·정합성 요구를 적는다.
검증 방법이 없는 비기능 요구사항은 적지 않은 것과 같다.
-->

| ID | 요구사항 | 출처 (S- / H-) | 대응 방식 | 검증 |
|---|---|---|---|---|
| NFR-1 | 중복 요청 방지 (멱등성) | | | TC- |
| NFR-2 | 동시 요청 시 데이터 정합성 | | → [02 §7](02-domain-model.md) | TC- |
| NFR-3 | 여러 단계 중 하나라도 실패하면 전체 롤백 | | | TC- |
| NFR-4 | 멀티 인스턴스 환경 동작 | | 상태를 DB에만 둔다 | 설계 근거 |

---

## 4. 제출물 요구사항

<!-- 원문에 적힌 제출물 항목을 빠짐없이 옮긴다. README에서 누락되기 쉽다 -->

| ID | 요구사항 | 산출물 위치 | 완료 |
|---|---|---|---|
| SUB-1 | 실행 방법 | README | [ ] |
| SUB-2 | 기술 스택 **및 선택 이유** | README | [ ] |
| SUB-3 | API 명세 | README / Swagger | [ ] |
| SUB-4 | AI 활용 내역 | `docs/ai-log/README.md` (원본: `docs/ai-log/F*.md`) | [ ] |

---

## 5. 모호한 점과 결정

<!--
분석 §4의 C-n 중 사용자가 정한 것(분석 §7)을 옮긴다.
결정은 사용자가 내린다. 문서 작성자가 고르거나 추천하지 않는다.
보류된 결정은 결정 칸에 "보류"라고 쓴다.
면접 질문은 대부분 여기서 나온다.
"왜 이렇게 해석했나"에 한 줄로 답할 수 있어야 한다.
-->

### D-1. {쟁점 한 줄}

- 관련: C- · FR-
- 원문: S- "…"
- 선택지
  - A: … — 장점 / 단점
  - B: … — 장점 / 단점
- **결정**: 
- **근거**: 
- 남는 한계: 

---

## 6. 커버리지 점검

설계를 끝내기 전에 확인한다. `verifier`도 같은 기준으로 검사한다.

- [ ] 분석의 모든 `S-n`이 `FR`·`NFR`·`SUB` 중 하나 이상으로 옮겨졌다
- [ ] 모든 `H-n`이 요구사항으로 옮겨졌거나 §1 범위 밖에 적혔다
- [ ] 분석 §7의 모든 결정이 §5 `D-n`으로 옮겨졌다
- [ ] 모든 `FR`에 테스트 케이스가 하나 이상 있다
- [ ] "~만 가능", "~해야 함" 같은 제약마다 **실패** 케이스가 있다
- [ ] 동시성·멱등성 `NFR`마다 **동시성** 케이스가 있다
- [ ] 모든 `D`가 관련 `FR`에 연결됐다
- [ ] 범위 밖으로 뺀 항목이 1절에 적혀 있다
