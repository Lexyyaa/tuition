# F{번호}. {기능명} `feature/{name}`

> 기능(F) 하나가 끝날 때 `docs/ai-log/F{번호}-{name}.md`로 복사해 작성한다.
> 목적
> - AI 작업을 블랙박스로 두지 않는다.
> - 나중에 "왜 이렇게 했나"에 답할 근거를 남긴다.

## 받은 지시

- (요청 원문 또는 요약)

## 작업 흐름

1. 

## 바뀐 파일

- `path` (생성/수정) — 한 줄 설명

## 설계대로 한 것

- 

## 설계에 없어서 정한 것

- 무엇을 / 선택지 / 고른 이유 (없으면 "없음")

## 설계와 다르게 간 것

- 무엇을 / 왜 (없으면 "없음")

## 검증

- `./gradlew spotlessApply build` → 
- `.http` 실측 → (케이스 · 응답 본문 · DB 확인 결과)

## 에러와 해결

- 에러 / 원인 / 해결 (없으면 "없음")

## 자주 틀리는 것 후보

- (이번에 AI가 틀린 것 중 올릴 만한 것)
  - 대상: 루트 CLAUDE.md "자주 틀리는 것"
  - 없으면 "없음"
